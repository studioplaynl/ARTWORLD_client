// eslint-disable-next-line no-unused-vars
class Achievements {
  // constructor() {
  // }
  // list of achievements
  // onBoardApp   || Did user ever log in? || Loging 1x
  // onBoardAvatar || Did user go through avatar tutoral || inGame Clicking or session of Avatar App
  // onBoardHouse  || Did user go through house tutoral || inGame Clicking or session of House App
  // madeDrawing   || Did user save a drawing for the first time || User has more than 0 drawings
  // madeAnimation || Did user save a animation for the first time || User has move
  // same princile for 5 /10 etc amount
  // visitedHouse  || Visisted a house of on other user || L
}
