import { writable } from 'svelte/store';

export const playerPosX = writable(0);
export const playerPosY = writable(0);
// export const playerLocationScene = writable('');
// export const playerLocationHouse = writable('');

