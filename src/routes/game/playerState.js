import { writable } from 'svelte/store';

export const playerPosX = writable(null);
export const playerPosY = writable(null);
export const playerLocation = writable(null);
// export const playerLocationHouse = writable('');

